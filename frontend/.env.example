# Copy this file to .env and adjust as needed.
# Never commit the real .env file — it is git-ignored.

# Base URL for the LifeOS API.
# In development, Vite proxies /api to the backend, so "/api" works out of the box.
VITE_API_URL=/api